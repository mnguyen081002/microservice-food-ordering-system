apiVersion: v1
kind: Service
metadata:
  name: {{ include "fos.fullname" . }}
  labels:
    {{- include "fos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fos.selectorLabels" . | nindent 4 }}
