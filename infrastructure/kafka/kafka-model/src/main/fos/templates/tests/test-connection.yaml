apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fos.fullname" . }}-test-connection"
  labels:
    {{- include "fos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
